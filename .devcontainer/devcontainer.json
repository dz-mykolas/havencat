{
  "name": "havenchat",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "remoteUser": "vscode",
  "postCreateCommand": "fvm install && fvm flutter pub get && fvm flutter doctor -v",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter"
      ],
      "settings": {
        "dart.flutterSdkPath": ".fvm/flutter_sdk",
        "dart.checkForSdkUpdates": false,
        "editor.formatOnSave": true
      }
    }
  }
}