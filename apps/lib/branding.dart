/// Single source of truth for the app's user-facing brand name.
///
/// Import [appName] instead of hardcoding the product name anywhere in the
/// UI, server logs, or tests.
const String appName = 'HavenCat';
